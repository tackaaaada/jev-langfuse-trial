[project]
name = "jev-langfuse-trial"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "langfuse>=4.15.4",
    "python-dotenv>=1.2.3",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "ruff>=0.16.8",
]
